<Window x:Class="TaskEngine.PcGraphWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:scottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        Title="PcGraphWindow" Height="720" Width="1100"
        Background="#0B0B0B" 
        WindowStartupLocation="CenterScreen">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock x:Name="TitleText"
                   Grid.Row="0"
                   Text="Gráficos - PC"
                   Foreground="White"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="6,0,6,12"/>

        <!-- Informativo -->
        <TextBlock Grid.Row="1"
                   Text="Últimos 14 días • Actualización automática cada hora"
                   Foreground="#CCCCCC"
                   FontSize="12"
                   Margin="6,0"/>

        <!-- Gráficos -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- CPU -->
            <Border Grid.Row="0" Grid.Column="0" Background="#0B0B0B" BorderBrush="#1E1E1E" BorderThickness="1" CornerRadius="6" Padding="6" Margin="0,0,6,6">
                <scottPlot:WpfPlot x:Name="CpuPlot"
                                   PreviewMouseWheel="BlockMouse"
                                   PreviewMouseDown="BlockMouse"
                                   PreviewMouseMove="BlockMouse"
                                   Focusable="False"/>
            </Border>

            <!-- RAM -->
            <Border Grid.Row="0" Grid.Column="1" Background="#0B0B0B" BorderBrush="#1E1E1E" BorderThickness="1" CornerRadius="6" Padding="6" Margin="6,0,0,6">
                <scottPlot:WpfPlot x:Name="RamPlot"
                                   PreviewMouseWheel="BlockMouse"
                                   PreviewMouseDown="BlockMouse"
                                   PreviewMouseMove="BlockMouse"
                                   Focusable="False"/>
            </Border>

            <!-- TEMP -->
            <Border Grid.Row="1" Grid.Column="0" Background="#0B0B0B" BorderBrush="#1E1E1E" BorderThickness="1" CornerRadius="6" Padding="6" Margin="0,6,6,0">
                <scottPlot:WpfPlot x:Name="TempPlot"
                                   PreviewMouseWheel="BlockMouse"
                                   PreviewMouseDown="BlockMouse"
                                   PreviewMouseMove="BlockMouse"
                                   Focusable="False"/>
            </Border>

            <!-- DISK -->
            <Border Grid.Row="1" Grid.Column="1" Background="#0B0B0B" BorderBrush="#1E1E1E" BorderThickness="1" CornerRadius="6" Padding="6" Margin="6,6,0,0">
                <scottPlot:WpfPlot x:Name="DiskPlot"
                                   PreviewMouseWheel="BlockMouse"
                                   PreviewMouseDown="BlockMouse"
                                   PreviewMouseMove="BlockMouse"
                                   Focusable="False"/>
            </Border>
        </Grid>
    </Grid>
</Window>